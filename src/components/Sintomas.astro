---
import { Stethoscope } from '@lucide/astro';
---

<div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <!-- Formulario de Síntomas -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-300">
        <div class="p-6">
            <h2 class="text-xl font-semibold flex items-center gap-2">
                <span class="inline-block w-5 h-5 text-blue-600">
                    <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4"></path>
                    </svg>
                </span>
                Agregar Síntoma
            </h2>
            <p class="text-gray-600 mt-1">
                Registra un nuevo síntoma con su descripción
            </p>
        </div>
        <div class="pb-6 pr-6 pl-6">
            <form id="sintomas-form" class="space-y-4">
                <div>
                    <label for="sintoma-nombre" class="block text-sm font-medium text-gray-700 mb-2">
                        Nombre del Síntoma
                    </label>
                    <input type="text" id="sintoma-nombre" placeholder="Dolor de Cabeza" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" required />
                </div>
                <div>
                    <label for="sintoma-descripcion" class="block text-sm font-medium text-gray-700 mb-2">
                        Descripción
                    </label>
                    <textarea id="sintoma-descripcion" placeholder="Es una de las afecciones más comunes y puede tener diversas causas, desde estrés o mala postura hasta migrañas o problemas de salud más graves, por lo que es importante reconocer su tipo y saber cuándo buscar atención médica." rows="5" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" required></textarea>
                </div>
                <div class="flex gap-2">
                    <button type="submit" class="flex-1 bg-blue-800 text-white py-2 px-4 rounded-md hover:bg-blue-950 transition-colors flex items-center justify-center gap-2">
                        <span class="inline-block w-4 h-4 text-white">
                            <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                                <path stroke-linecap="round"  stroke-linejoin="round" d="M12 4v16m8-8H4"></path>
                            </svg>
                        </span>
                        Agregar
                    </button>
                    <button type="button" class="flex-1 bg-yellow-500 text-white py-2 px-4 rounded-md hover:bg-yellow-600 transition-colors flex items-center justify-center gap-2">
                        <span class="inline-block w-4 h-4 text-white">
                            <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M15.232 5.232l3.536 3.536M9 11l6 6M3 21h18"></path>
                            </svg>
                        </span>
                        Editar
                    </button>
                    <button type="button" class="flex-1 bg-red-600 text-white py-2 px-4 rounded-md hover:bg-red-700 transition-colors flex items-center justify-center gap-2">
                        <span class="inline-block w-4 h-4 text-white">
                            <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
                            </svg>
                        </span>
                        Eliminar
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Lista de Síntomas -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-300">
        <div class="pr-6 pl-6 pt-6">
            <h2 class="text-xl font-semibold">
                <Stethoscope class="w-6 h-6 inline mr-1 text-blue-800" />
                Síntomas Registrados</h2>
            <p class="text-gray-600 mt-1">
                Total de síntomas: <span id="sintomas-count">0</span> síntomas
            </p>
            <!-- Buscador de síntomas -->
            <div class="mt-4">
                <input type="text" id="buscador-sintomas" placeholder="Buscar síntoma..." class="w-full px-1 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" />
            </div>
        </div>
        <div class="p-6">
            <!-- Tabla de síntomas -->
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200" id="tabla-sintomas">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Nombre</th>
                            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Descripción</th>
                        </tr>
                    </thead>
                    <tbody id="sintomas-list">
                        <tr>
                            {
                                /* Aquí se insertarán los síntomas dinámicamente */
                            }
                            <td colspan="2" class="text-gray-500 text-center py-4">
                                No hay síntomas registrados
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
